function Testing{
    declare parameter r.
    declare parameter s.
    declare parameter n.

    local a to -1/cos(n).

    local b to r / (2*s).

    local c to 1/2 * sqrt().

    local d to r^2/s^2.
    

    local result to b - c.
}